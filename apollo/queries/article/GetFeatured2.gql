query getFeatured2($slug: String) {
  category(id: 5) {
    articles(where: { slug: $slug }) {
      id
      slug
      title
      content
      excerpt
      created_at
      image {
        url
      }
      created_by {
        username
      }
    }
    name
    id
  }
}